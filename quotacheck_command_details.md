# quotacheck Command - Detailed Documentation

## 1. Description
The `quotacheck` command scans a filesystem for disk usage, creates, checks and repairs quota files.

## 2. Purpose
The purpose of `quotacheck` is quota maintenance.

## 3. Examples
### Example 1: Create Database
```bash
sudo quotacheck -cug /mount/point
```
