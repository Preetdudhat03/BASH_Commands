# mountpoint Command - Detailed Documentation

## 1. Description
The `mountpoint` command checks if the directory is a mountpoint.

## 2. Purpose
The purpose of `mountpoint` is scripting.

## 3. Examples
### Example 1: Check
```bash
mountpoint /mnt/backup
```
