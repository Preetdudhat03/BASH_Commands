# unlink Command - Detailed Documentation

## 1. Description
The `unlink` command removes a file. It calls the `unlink` function.

## 2. Purpose
The purpose of `unlink` is file removal.

## 3. Examples
### Example 1: Unlink
```bash
unlink file.txt
```
