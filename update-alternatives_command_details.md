# update-alternatives Command - Detailed Documentation

## 1. Description
The `update-alternatives` command maintains symbolic links determining default commands.

## 2. Purpose
The purpose of `update-alternatives` is default selection.
- Choose which version of `java` or `python` runs when you type the command.

## 3. Examples
### Example 1: Config Editor
```bash
sudo update-alternatives --config editor
```
