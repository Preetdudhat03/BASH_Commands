# insmod Command - Detailed Documentation

## 1. Description
The `insmod` command inserts a module into the Linux Kernel.

## 2. Purpose
The purpose of `insmod` is manual loading. `modprobe` is generally preferred.

## 3. Examples
### Example 1: Insert
```bash
sudo insmod /path/to/module.ko
```
