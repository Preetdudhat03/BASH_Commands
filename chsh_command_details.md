# chsh Command - Detailed Documentation

## 1. Description
The `chsh` command changes the user's login shell.

## 2. Purpose
The purpose of `chsh` is preference. It is used to:
- Switch from `bash` to `zsh` as default.

## 3. Examples
### Example 1: Change to ZSH
```bash
chsh -s /bin/zsh
```
