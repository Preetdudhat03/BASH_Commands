# raw Command - Detailed Documentation

## 1. Description
The `raw` command binds a Linux raw character device.

## 2. Purpose
The purpose of `raw` is raw device mapping (legacy).

## 3. Examples
### Example 1: Bind
```bash
sudo raw /dev/raw/raw1 /dev/sda1
```
