# partprobe Command - Detailed Documentation

## 1. Description
The `partprobe` command informs the OS of partition table changes.

## 2. Purpose
The purpose of `partprobe` is re-reading partition tables without reboot.

## 3. Examples
### Example 1: Probe
```bash
sudo partprobe
```
