# bzip2 Command - Detailed Documentation

## 1. Description
The `bzip2` command compresses files. It typically compresses better than gzip but is slower. Files end in `.bz2`.

## 2. Purpose
The purpose of `bzip2` is high compression.

## 3. Examples
### Example 1: Compress
```bash
bzip2 file.txt
```
