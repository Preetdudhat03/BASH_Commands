# popd Command - Detailed Documentation

## 1. Description
The `popd` command removes the top directory from the stack saved by `pushd` and returns you to it.

## 2. Purpose
The purpose of `popd` is return navigation.

## 3. Examples
### Example 1: Return
Go back to the previous folder.
```bash
popd
```
