# env Command - Detailed Documentation

## 1. Description
The `env` command runs a program in a modified environment or prints the current environment variables.

## 2. Purpose
The purpose of `env` is environment check. It is used to:
- See all set variables.
- Run a command with clean variables (`env -i`).

## 3. Examples
### Example 1: List Variables
```bash
env
```
