# ssh-copy-id Command - Detailed Documentation

## 1. Description
The `ssh-copy-id` command installs your public key in a remote machine's authorized_keys.

## 2. Purpose
The purpose of `ssh-copy-id` is easily enabling passwordless login.

## 3. Examples
### Example 1: Install Key
```bash
ssh-copy-id user@remote_host
```
