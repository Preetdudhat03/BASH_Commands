# rg Command - Detailed Documentation

## 1. Description
The `rg` (Ripgrep) command is a line-oriented search tool that recursively searches the current directory. It is extremely fast.

## 2. Purpose
The purpose of `rg` is fastest search.

## 3. Examples
### Example 1: Find string
```bash
rg "string"
```
