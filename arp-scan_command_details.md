# arp-scan Command - Detailed Documentation

## 1. Description
The `arp-scan` command sends ARP packets to hosts on the local subnet to find active devices.

## 2. Purpose
The purpose of `arp-scan` is discovering local network devices.

## 3. Examples
### Example 1: Scan Local
```bash
sudo arp-scan --localnet
```
