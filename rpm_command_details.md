# rpm Command - Detailed Documentation

## 1. Description
The `rpm` command is the RPM Package Manager.

## 2. Purpose
The purpose of `rpm` is low-level package management.

## 3. Examples
### Example 1: Install RPM
```bash
sudo rpm -i file.rpm
```
