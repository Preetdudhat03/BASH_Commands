# switch_root Command - Detailed Documentation

## 1. Description
The `switch_root` command switches to another filesystem as the root of the mount tree.

## 2. Purpose
The purpose of `switch_root` is typically used in initramfs scripts.

## 3. Examples
### Example 1: Switch
```bash
switch_root /newroot /sbin/init
```
