# visudo Command - Detailed Documentation

## 1. Description
The `visudo` command edits the `/etc/sudoers` file in a safe, secure manner. It checks syntax before saving.

## 2. Purpose
The purpose of `visudo` is safe config. **Always use this instead of directly editing sudoers.**

## 3. Examples
### Example 1: Edit
```bash
sudo visudo
```
