# btrfs Command - Detailed Documentation

## 1. Description
The `btrfs` command is a control tool for the Btrfs filesystem.

## 2. Purpose
The purpose of `btrfs` is managing snapshots, subvolumes, and raid.

## 3. Examples
### Example 1: Filesystem usage
```bash
sudo btrfs filesystem usage /mnt/data
```
