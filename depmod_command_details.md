# depmod Command - Detailed Documentation

## 1. Description
The `depmod` command generates `modules.dep` and map files.

## 2. Purpose
The purpose of `depmod` is handling kernel module dependencies.

## 3. Examples
### Example 1: Scan
```bash
sudo depmod -a
```
