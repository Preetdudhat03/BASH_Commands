# gpasswd Command - Detailed Documentation

## 1. Description
The `gpasswd` command administers /etc/group.

## 2. Purpose
The purpose of `gpasswd` is managing group memberships and passwords.

## 3. Examples
### Example 1: Add User
```bash
sudo gpasswd -a user group
```
