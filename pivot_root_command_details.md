# pivot_root Command - Detailed Documentation

## 1. Description
The `pivot_root` command changes the root filesystem.

## 2. Purpose
The purpose of `pivot_root` is switching the root mount during boot.

## 3. Examples
### Example 1: Usage
```bash
pivot_root . old-root
```
