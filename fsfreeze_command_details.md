# fsfreeze Command - Detailed Documentation

## 1. Description
The `fsfreeze` command suspends access to a filesystem.

## 2. Purpose
The purpose of `fsfreeze` is halting filesystem IO for snapshots.

## 3. Examples
### Example 1: Freeze
```bash
sudo fsfreeze -f /mnt
```
