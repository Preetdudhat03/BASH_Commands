# dpkg Command - Detailed Documentation

## 1. Description
The `dpkg` command is a tool to install, build, remove, and manage Debian packages. It is the backend for `apt`.

## 2. Purpose
The purpose of `dpkg` is local package install.

## 3. Examples
### Example 1: Install .deb
```bash
sudo dpkg -i package.deb
```
