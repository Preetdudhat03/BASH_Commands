# numactl Command - Detailed Documentation

## 1. Description
The `numactl` command controls NUMA policy for processes or shared memory.

## 2. Purpose
The purpose of `numactl` is binding processes to specific CPUs/memory nodes.

## 3. Examples
### Example 1: Show
```bash
numactl --show
```
