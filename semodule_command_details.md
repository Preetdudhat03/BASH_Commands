# semodule Command - Detailed Documentation

## 1. Description
The `semodule` command manages SELinux policy modules.

## 2. Purpose
The purpose of `semodule` is installing/removing policy modules.

## 3. Examples
### Example 1: List
```bash
sudo semodule -l
```
