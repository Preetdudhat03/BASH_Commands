# yum Command - Detailed Documentation

## 1. Description
The `yum` command is an interactive, rpm-based package manager (RHEL/CentOS).

## 2. Purpose
The purpose of `yum` is package management.

## 3. Examples
### Example 1: Install
```bash
sudo yum install httpd
```
