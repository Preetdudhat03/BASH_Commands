# ltrace Command - Detailed Documentation

## 1. Description
The `ltrace` command is a library call tracer. It intercepts and records dynamic library calls which are called by the executed process.

## 2. Purpose
The purpose of `ltrace` is userspace debugging.

## 3. Examples
### Example 1: Trace
```bash
ltrace ls
```
