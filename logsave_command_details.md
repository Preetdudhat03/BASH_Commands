# logsave Command - Detailed Documentation

## 1. Description
The `logsave` command saves the output of a command in a logfile.

## 2. Purpose
The purpose of `logsave` is saving boot logs (keeps running if disk absent).

## 3. Examples
### Example 1: Run
```bash
logsave /tmp/log.txt ls
```
