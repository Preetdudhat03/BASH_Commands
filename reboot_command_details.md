# reboot Command - Detailed Documentation

## 1. Description
The `reboot` command restarts the system. It is essentially a shortcut for `shutdown -r now`.

## 2. Purpose
The purpose of `reboot` is restarting.

## 3. Examples
### Example 1: Restart
```bash
sudo reboot
```
