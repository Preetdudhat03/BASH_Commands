# snap Command - Detailed Documentation

## 1. Description
The `snap` command is used to install, configure, verify, and remove snap packages. Snaps are universal Linux packages.

## 2. Purpose
The purpose of `snap` is universal apps.

## 3. Examples
### Example 1: Install
```bash
sudo snap install spotify
```
