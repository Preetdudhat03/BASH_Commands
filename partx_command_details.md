# partx Command - Detailed Documentation

## 1. Description
The `partx` command tells the kernel about the presence and numbering of on-disk partitions.

## 2. Purpose
The purpose of `partx` is reading partition tables.

## 3. Examples
### Example 1: Show
```bash
sudo partx --show /dev/sda
```
