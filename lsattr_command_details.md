# lsattr Command - Detailed Documentation

## 1. Description
The `lsattr` command lists file attributes on a Linux second extended file system.

## 2. Purpose
The purpose of `lsattr` is verification. It is used to:
- Check if a file is immutable (`i`).

## 3. Examples
### Example 1: List Attributes
```bash
lsattr file.txt
```
