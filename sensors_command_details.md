# sensors Command - Detailed Documentation

## 1. Description
The `sensors` command displays text-based info from hardware sensors (voltage, fan speed, temperature). Needs `lm-sensors`.

## 2. Purpose
The purpose of `sensors` is hardware health. Check if the CPU is overheating.

## 3. Examples
### Example 1: Check Temps
```bash
sensors
```
