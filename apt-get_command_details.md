# apt-get Command - Detailed Documentation

## 1. Description
The `apt-get` command is the command-line tool for handling packages. It is lower level than `apt` and preferred for scripts.

## 2. Purpose
The purpose of `apt-get` is scripting package management.

## 3. Examples
### Example 1: Install
```bash
sudo apt-get install -y git
```
