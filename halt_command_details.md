# halt Command - Detailed Documentation

## 1. Description
The `halt` command stops the system. It is roughly equivalent to `shutdown -h`.

## 2. Purpose
The purpose of `halt` is stopping.

## 3. Examples
### Example 1: Halt
```bash
sudo halt
```
