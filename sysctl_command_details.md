# sysctl Command - Detailed Documentation

## 1. Description
The `sysctl` command configures kernel parameters at runtime.

## 2. Purpose
The purpose of `sysctl` is kernel tuning.

## 3. Examples
### Example 1: List All
```bash
sysctl -a
```
