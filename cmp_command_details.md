# cmp Command - Detailed Documentation

## 1. Description
The `cmp` command compares two files byte by byte.

## 2. Purpose
The purpose of `cmp` is binary comparison. It is used to:
- Find the exact byte where two files begin to differ.

## 3. Examples
### Example 1: Compare
```bash
cmp file1.bin file2.bin
```
