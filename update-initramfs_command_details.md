# update-initramfs Command - Detailed Documentation

## 1. Description
The `update-initramfs` command generates an initramfs image.

## 2. Purpose
The purpose of `update-initramfs` is refreshing boot images.

## 3. Examples
### Example 1: Update
```bash
sudo update-initramfs -u
```
