# pvcreate Command - Detailed Documentation

## 1. Description
The `pvcreate` command initializes physical volumes for use by LVM (Logical Volume Manager).

## 2. Purpose
The purpose of `pvcreate` is LVM initialization.

## 3. Examples
### Example 1: Init Disk
```bash
sudo pvcreate /dev/sdb
```
