# fold Command - Detailed Documentation

## 1. Description
The `fold` command wraps each input line to fit in specified width. Unlike `fmt`, it breaks words if necessary.

## 2. Purpose
The purpose of `fold` is hard wrapping.

## 3. Examples
### Example 1: Wrap
```bash
fold -w 80 file.txt
```
