# tac Command - Detailed Documentation

## 1. Description
The `tac` command concatenates and prints files in reverse (last line first). It is `cat` backwards.

## 2. Purpose
The purpose of `tac` is reverse reading. Useful for reading logs from newest to oldest.

## 3. Examples
### Example 1: Reverse
```bash
tac file.txt
```
