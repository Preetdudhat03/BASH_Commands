# envsubst Command - Detailed Documentation

## 1. Description
The `envsubst` command substitutes environment variables in shell format strings.

## 2. Purpose
The purpose of `envsubst` is templating with environment vars.

## 3. Examples
### Example 1: Sub
```bash
echo '$HOME' | envsubst
```
