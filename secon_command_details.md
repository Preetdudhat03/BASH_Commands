# secon Command - Detailed Documentation

## 1. Description
The `secon` command is used to see the context of the process, file, or input.

## 2. Purpose
The purpose of `secon` is inspecting security contexts components.

## 3. Examples
### Example 1: File
```bash
secon -f /etc/passwd
```
