# xfs_growfs Command - Detailed Documentation

## 1. Description
The `xfs_growfs` command expands an existing XFS filesystem.

## 2. Purpose
The purpose of `xfs_growfs` is resizing XFS filesystems online.

## 3. Examples
### Example 1: Grow
```bash
sudo xfs_growfs /mount/point
```
